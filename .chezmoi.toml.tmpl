{{- $machineType := promptChoiceOnce . "machineType" "Machine type" (list "normal" "server") -}}
[data]
  machineType = {{ $machineType | quote }}

[hooks.read-source-state.pre]
  command = "{{ .chezmoi.sourceDir }}/.install-prerequisites.sh"
